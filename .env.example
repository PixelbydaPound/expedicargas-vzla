VITE_GOOGLE_MAPS_API_KEY=your_google_maps_api_key_here

# Contact form (Vercel serverless — set in Vercel project env, not exposed to browser)
RESEND_API_KEY=your_resend_api_key_here
# Must use a verified domain in Resend, e.g. after adding expedicargas.com at resend.com/domains
RESEND_FROM=Expedicargas <info@expedicargas.com>
CONTACT_FORM_TO=evelyngonzalez@expedicargas.com
# For testing only (before domain verification): use onboarding@resend.dev as RESEND_FROM
# and set CONTACT_FORM_TO to the email address on your Resend account.
